import React, { createContext } from 'react'

const Asteroids = (useEffect, update) => {
  let asteroids = [];
  
  let drawAsteroids = (update) => {
    ctx.strokeStyle = 'slategrey';
    ctx.lineWidth = ShipSize / 20
    for (let i = 0; i < asteroids.length; i++){

    }
  }

    return (
    <div>Asteroids</div>
  )
}

export default Asteroids